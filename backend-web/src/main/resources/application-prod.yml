server:
  servlet:
    context-path: /teamachinebackend
  port: 80

spring:
  redis:
    host: 127.0.0.1
    port: 6379
    # username: r-bp13wg6c4rk6hn0rio
    password: langtuopassword

teamachine:
  database:
    host: rm-cn-28t3ppz9e0001yho.rwlb.rds.aliyuncs.com
    port: 3306
    dbname: gx_mysql_demo
    username: miya
    password: password@1
  jwt:
    tokenHeader: Authorization # JWT 存储的请求头
    secret: teamachine-secret # JWT 加解密使用的密钥
    expiration: 604800 # JWT 的超期限时间，单位分钟(60*60*24*7)
    tokenHead: 'Bearer '  # JWT 负载中拿到开头
  secure:
    ignored:
      urls: # 安全路径白名单，不包含 tomcat 中 context 的 path（从之后开始算）
        - /swagger-ui/
        - /swagger-resources/**
        - /**/*.html
        - /**/*.js
        - /**/*.css
        - /**/*.png
        - /**/*.map
        - /favicon.ico
        - /actuator/**
        - /druid/**
        - /login/**
        - /logout/**
        - /userset/tenant/list/**
        - /deviceset/machine/activate/**
        - /mqtt/**
